install_items+=" /etc/luks/empty-keyfile "
# In generic (non-hostonly) mode, the crypt module's install() doesn't
# auto-embed /etc/crypttab — that branch is gated on hostonly. Force it
# in so systemd-cryptsetup-generator at boot has something to act on.
install_items+=" /etc/crypttab "
